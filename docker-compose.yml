version: '3'

networks:
  cerberus_net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/24

services:
  # 1. THE GATEWAY (Guacamole)
  guacamole:
    # OLD image: oznu/guacamole:latest
    # NEW image: jwetzell/guacamole:latest
    image: jwetzell/guacamole:latest 
    container_name: cerberus_gateway
    ports:
      - "127.0.0.1:8080:8080"
    networks:
      cerberus_net:
        ipv4_address: 172.20.0.2
    environment:
      - EXT_AUTH=n
      - HOME=/config
    restart: always
    volumes:
      - ./config:/config

  # 2. THE ATTACKER (Custom Kali Build)
  operator:
    build: .
    container_name: cerberus_operator
    privileged: true
    tty: true
    networks:
      cerberus_net:
        ipv4_address: 172.20.0.10
    cap_add:
      - NET_ADMIN
    volumes:
      - ./cerberus_core:/app
      - ./reports:/app/reports

  # 3. THE TARGET (Ubuntu)
  target:
    image: ubuntu:20.04
    container_name: cerberus_target
    networks:
      cerberus_net:
        ipv4_address: 172.20.0.20
    command: sleep infinity
    privileged: true
    restart: always
    volumes:
      - ./my_project:/app